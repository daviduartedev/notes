# Copie para `.env` localmente. Nunca commite `.env`.
# Valores abaixo são placeholders — não são secrets de produção.

DATABASE_URL=postgresql://notes:notes@localhost:5433/notes
AUTH_SECRET=replace-with-at-least-32-characters-long
WEB_ORIGIN=http://localhost:3015
API_ORIGIN=http://localhost:3014
AUTH_URL=http://localhost:3014
NEXT_PUBLIC_API_ORIGIN=http://localhost:3014

SEED_OWNER_EMAIL=owner@example.com
SEED_OWNER_PASSWORD=changeme
SEED_WORKSPACE_NAME=Notes
